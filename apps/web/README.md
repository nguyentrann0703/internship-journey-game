# Web App Placeholder

Place the frontend app here.

Recommended shape if you use Next.js:

```text
apps/web/
  app/
  components/
  lib/
  public/
```

Frontend should import shared contracts from `../../src/shared/contracts.ts` until a package boundary is introduced.
